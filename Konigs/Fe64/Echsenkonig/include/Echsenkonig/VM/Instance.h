/**
 * Konigs Infrastructure by Ruka Raposa Team & Contribuitors.
 * Project is under GPL3-license, read `LICENSE.md` on the project root.
 */
#ifndef EGVMInstance_h
#define EGVMInstance_h

#include "Echsenkonig/Types.h"

/**
 * @brief The instance contains the memory and the threads the machine is
 * operating with. Each instance is independent from the other, each thread
 * inside an instance shares the same memory, but an instance can share a
 * different memory object.
 */
typedef struct EG_Instance
{
    
} EG_Instance;

#endif